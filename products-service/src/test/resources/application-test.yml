spring:
  datasource:
    url: jdbc:tc:postgresql:16:///productsdb
    username: products
    password: products
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: public

logging:
  level:
    org.testcontainers: INFO
